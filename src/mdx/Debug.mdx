## 查看焦点位置是否正确

在控制台运行以下代码

{/* ```js filename="awesome.js" ok={true} onOpen="true" */}

```js
document.addEventListener(
  "focus",
  (event) => {
    console.log("Focused element:", event.target);
  },
  true
);
```
