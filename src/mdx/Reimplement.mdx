import { DebounceComponent } from "./Reimplement/Reimplement";

# 手写系列

## 手写 Javascript 函数

### debounce

```js
// no-collapse
function debounce(func, delay) {
  let timeoutId;

  return function (...args) {
    // 清除之前的定时器
    clearTimeout(timeoutId);

    // 设置新的定时器
    timeoutId = setTimeout(() => {
      func.apply(this, args);
    }, delay);
  };
}
```

用法: (打开控制台查看 console 输出)

<DebounceComponent />

```tsx
// no-collapse
const debouncedSearch = debounce((searchTerm) => {
  console.log("Searching for:", searchTerm);
}, 300);

function DebounceComponent() {
  const handleInputChange = (event: ChangeEvent<HTMLInputElement>) => {
    debouncedSearch(event.target.value);
  };

  return <Input type="text" onChange={handleInputChange} className="w-36" />;
}
```

疑惑: 为什么需要 `apply(this, args)` 而不是直接 `func(args)`?
{/* TODO: 深入理解this */}

```js
const user = {
  name: "Alice",
  greet: function () {
    console.log(`Hello, I'm ${this.name}`);
  },
};

// const debouncedGreet = debounce(user.greet, 1000); // 不行
const debouncedGreet = debounce(() => user.greet(), 1000); // 可行

// 如果不正确处理 this，这里可能输出 "Hello, I'm undefined"
debouncedGreet();
```

### throttle

```js
// no-collapse
function throttle(func, limit) {
  let lastFunc;
  let lastRan;
  return function (...args) {
    if (!lastRan) {
      func.apply(this, args);
      lastRan = Date.now();
    } else {
      clearTimeout(lastFunc);
      lastFunc = setTimeout(() => {
        if (Date.now() - lastRan >= limit) {
          func.apply(this, args);
          lastRan = Date.now();
        }
      }, limit - (Date.now() - lastRan));
    }
  };
}
```

### flat

```js
// no-collapse
function flat(arr, depth = 1) {
  return arr.reduce((acc, cur) => {
    if (Array.isArray(cur) && depth > 0) {
      acc.push(...flat(cur, depth - 1));
    } else {
      acc.push(cur);
    }
    return acc;
  }, []);
}
```

### 浅拷贝&深拷贝

shallow copy

```js
// no-collapse
// 使用 Object.assign
let obj = { a: 1, b: { c: 2 } };
let shallowCopy = Object.assign({}, obj);

// 使用扩展运算符
let original = { a: 1, b: { c: 2 } };
let copy = { ...original };

// 对于数组
let originalArray = [1, 2, { a: 3 }];
let copyArray = [...originalArray];
```

deep copy

```js
// no-collapse
// 使用 JSON 方法（有局限性）
const original = { a: 1, b: { c: 2 } };
const deep = JSON.parse(JSON.stringify(original));
console.log(deep);
```

{/* NOTE: https://chatgpt.com/share/670fa387-d1e4-8012-8230-a15c2800e629 */}

```js
// no-collapse
// 递归实现（简单版，不处理循环引用等复杂情况）
function deepCopy(obj) {
  if (typeof obj !== "object" || obj === null) {
    return obj;
  }
  let newObj = Array.isArray(obj) ? [] : {};
  for (let key in obj) {
    if (Object.prototype.hasOwnProperty.call(obj, key)) {
      newObj[key] = deepCopy(obj[key]);
    }
  }
  return newObj;
}

const original = { a: 1, b: { c: 2 } };
const deep = deepCopy(original);
console.log(deep);
```

### 日期格式化

实现 `format(new Date(), 'YYYY-MM-DD')` 函数

```js
// no-collapse
function format(date, formatString) {
  const year = date.getFullYear(); // 获取年份
  const month = String(date.getMonth() + 1).padStart(2, "0"); // 获取月份并补零
  const day = String(date.getDate()).padStart(2, "0"); // 获取日期并补零

  // 替换格式字符串中的占位符
  return formatString
    .replace("YYYY", year)
    .replace("MM", month)
    .replace("DD", day);
}

// 使用示例
const formattedDate = format(new Date(), "YYYY-MM-DD");
console.log(formattedDate); // 输出: 2024-10-16
```
